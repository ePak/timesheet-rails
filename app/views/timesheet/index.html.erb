<h1>Timesheet#index</h1>
<p>Find me in app/views/timesheet/index.html.erb</p>
<div id="app"></div>
<% @projects.each do |project| %>
  <div>Project -> key: <%= project.key %>, name: <%= project.name %></div>
<% end %>
<% @logs.each do |log| %>
  <div>TimeLog -> date: <%= log.date %>, CR: <%= log.key %>, hours <%= log.hours %>, name: <%= log.name %></div>
<% end %>
